# 📋 הוראות עדכון Google Apps Script

## שלב 1: פתיחת Apps Script
1. פתחי את Google Sheets שלך
2. לחצי על **Extensions** → **Apps Script**
3. אמורה להיפתח הקונסולה עם הקוד הקיים

## שלב 2: העתקת הקוד החדש
1. פתחי את הקובץ **apps-script-update.js** ב-VS Code
2. העתיקי את **כל התוכן** של הקובץ (Ctrl+A ואז Ctrl+C)
3. חזרי לקונסולת Apps Script
4. גללי לגמרי למטה בקובץ Code.gs
5. הוסיפי שורה ריקה
6. הדביקי את כל התוכן (Ctrl+V)

## שלב 3: שמירה ופרסום
1. שמרי את הקוד: **Ctrl+S** או סמל השמירה
2. לחצי על **Deploy** → **Manage deployments**
3. לחצי על ⚙️ ליד הפריסה הפעילה
4. לחצי על **New version**
5. הוסיפי תיאור: "Added admin panel features"
6. לחצי **Deploy**

## שלב 4: עדכון Google Sheets
ודאי שב-Google Sheets יש עמודות בסדר הבא:
- A = תאריך
- B = מייל
- C = שם
- D = טלפון
- E = סוג (PURCHASE / INTERESTED)
- **F = סטטוס** ← **עמודה חדשה!** (הוסיפי אותה אם אין)
- G = הודעה
- H = מוצרים
- I = מחיר

### איך להוסיף עמודת סטטוס:
1. לחצי על עמודה F (אם יש שם מידע, תצטרכי להוסיף עמודה חדשה)
2. הקלידי בכותרת: **סטטוס**
3. בשורה 2 הקלידי: **לא טופל** (זאת ברירת המחדל)

## מה חדש בעדכון?

### ✨ תכונות חדשות:
1. **מיון לידים** - מהחדש לישן אוטומטית
2. **עמודת סטטוס** - "לא טופל" / "טופל"
3. **כפתור ייבוא לידים** - ייבוא מהמייל ישירות מהפאנל
4. **סה"כ מכירות** - card נוסף בדאשבורד עם סכום כסף
5. **עדכון סטטוס אוטומטי** - כשנשלח קובץ, הסטטוס משתנה ל"טופל"

### 🛠️ פונקציות חדשות שנוספו ל-Apps Script:
- `updateLeadStatusFromPanel()` - עדכון סטטוס ליד
- `extractLeadsFromPanel()` - ייבוא לידים מהמייל
- `getLeadsDataForPanel()` - טעינת לידים כולל סטטוס

## בדיקה שהכל עובד:
1. היכנסי לפאנל הניהול: https://haganenet-barbur.com/admin.html
2. סיסמה: **barbur2026**
3. בדקי שהלידים מוצגים מהחדש לישן
4. בדקי שיש card "סה"כ מכירות"
5. לחצי על "📧 ייבא מהמייל" ובדקי שזה עובד
6. לחצי על "📤 שלח קובץ" ליד ליד כלשהו
7. אחרי שליחת הקובץ, בדקי שהסטטוס השתנה ל"טופל"

## ❓ שאלות נפוצות

**ש: הלקוחות והקבצים לא נטענים בממשק שליחת קבצים**
ת: ודאי שהפונקציות `getCustomersList()` ו-`getFilesList()` מוגדרות בקוד המקורי של Apps Script. אם אין, הפאנל יטען מ-localStorage כגיבוי.

**ש: הניוזלטר נכשל**
ת: בדקי שהמשתנה `SIGNATURE_FILE_ID` מוגדר בקוד ושהוא מצביע על תמונת החתימה בדרייב.

**ש: הייבוא מהמייל לא עובד**
ת: ודאי שהפונקציה `extractFormspreeLeads()` קיימת בקוד המקורי של Apps Script.

## 🔧 עזרה נוספת
אם משהו לא עובד, פתחי את הקונסולה בדפדפן (F12) ובדקי האם יש שגיאות.
