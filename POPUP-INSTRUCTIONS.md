# הוראות שימוש - חלון קופץ פברואר 2026

## מה נוצר?
נוסף חלון קופץ (popup) מרשים עם אנימציית קונפטי שמכריז על תוכן חדש חינמי - "שולחנות תחושה לגן".

## תכונות החלון הקופץ:

### ✨ עיצוב ואנימציות
- **אנימציית קונפטי צבעונית** שיוצרת תחושה חגיגית
- **עיצוב מרשים** עם גרדיאנטים וצללים
- **וידאו preview** של המוצר שמתנגן אוטומטית
- **אנימציות כניסה** חלקות ומקצועיות
- **רספונסיבי מלא** - נראה מעולה בכל מכשיר

### 📅 לוגיקת הצגה
החלון יופיע **רק** כאשר:
1. ✅ התאריך הוא **פברואר 2026** (חודש 02/2026)
2. ✅ המשתמש **לא ראה** את החלון בסשן הנוכחי

### 🔄 התנהגות
- **לא יופיע ברענון דף** - אם המשתמש כבר ראה את החלון והוא עושה refresh, החלון לא יופיע שוב
- **סגירה** - ניתן לסגור עם:
  - כפתור X
  - לחיצה על הרקע האפור
  - מקש ESC
  - לחיצה על כפתור "לחנות הקבצים"

### 🎯 תוכן החלון
- כותרת: "🎉 תוכן חדש וחינמי! 🎉"
- וידאו של המוצר (19.mp4)
- תיאור המוצר: "שולחנות תחושה לגן!"
- מחיר מחוק: ₪5.00
- מחיר חינמי: "חינם!" (עם אנימציה)
- כפתור לחנות הקבצים

---

## ⚠️ חשוב! לפני העלאה לאתר

### שלב 1: בדיקה מקומית
**כרגע המודאל מוגדר להופיע תמיד** כדי שתוכלי לראות איך הוא נראה.

**כיצד לבדוק:**
1. פתחי את index.html בדפדפן
2. החלון הקופץ אמור להופיע אוטומטית אחרי 0.8 שניות
3. בדקי שהאנימציות עובדות
4. בדקי שהוידאו מתנגן
5. נסי לסגור בכל הדרכים (X, רקע, ESC, כפתור)
6. עשי refresh - המודאל לא אמור להופיע שוב

### שלב 2: הסרת מצב הבדיקה
**לפני העלאה לאתר בפועל**, יש להסיר את קוד הבדיקה!

**איך לעשות זאת:**

בקובץ `index.html`, מצאי את השורות (בערך שורה 1056):

```javascript
// *** לבדיקה בלבד - הסר את השורה הבאה אחרי הבדיקה! ***
return true; // זמני - להצגת המודאל עכשיו לבדיקה
// *** סוף קוד זמני ***
```

**מחקי את 3 השורות האלה לגמרי!**

הפונקציה צריכה להיראות כך:
```javascript
function isFebruary2026() {
  const now = new Date();
  const year = now.getFullYear();
  const month = now.getMonth(); // 0 = January, 1 = February
  return year === 2026 && month === 1;
}
```

---

## 🎨 התאמה אישית (אופציונלי)

### לשנות את התוכן
בקובץ `index.html`, חפשי את `<!-- February 2026 Popup Modal -->` ושני את:
- `popup-title` - הכותרת הראשית
- `popup-subtitle` - כותרת המשנה
- `popup-product-title` - שם המוצר
- `popup-description` - התיאור
- מסלול הוידאו: `images/Products/19.mp4`

### לשנות את התאריך
בפונקציה `isFebruary2026()`, שני:
- `year === 2026` למשהו אחר
- `month === 1` (1 = פברואר, 0 = ינואר, 2 = מרץ, וכו')

### לשנות צבעים
בסגנונות ה-CSS (בראש הקובץ), חפשי:
- `#7A1FA2` - הסגול הראשי
- `#9c4dbd` - הסגול הבהיר
- `#f3e9fb` - הרקע הבהיר

---

## 🐛 פתרון בעיות

### החלון לא מופיע
1. בדקי שזה פברואר 2026 (או שקוד הבדיקה פעיל)
2. נקי sessionStorage: פתחי Console בדפדפן והקלידי:
   ```javascript
   sessionStorage.removeItem('februaryPopupSeen2026');
   ```
3. רענני את הדף

### הוידאו לא מתנגן
1. ודאי שהקובץ `images/Products/19.mp4` קיים
2. בדפדפנים מסוימים וידאו לא יתנגן אוטומטית עד שהמשתמש יקליק במקום כלשהו בעמוד

### הקונפטי לא נראה
- הקונפטי מצויר על canvas, ודאי שה-JavaScript לא נחסם על ידי חוסם פרסומות

---

## 📋 רשימת קבצים שנערכו
- ✅ `index.html` - נוספו: CSS, HTML של המודאל, וקוד JavaScript

---

**בהצלחה! 🎉**
